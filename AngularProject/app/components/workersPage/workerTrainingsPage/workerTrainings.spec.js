'use strict';
describe('myApp.workers.trainings module', function () {
    beforeEach(module('myApp.workers.trainings'))
    describe('workers,trainings controller', function () {
        it('should ....', inject(function ($controller) {
            //spec body
            const TrainingsWorkersCtrl = $controller('TrainingsWorkersCtrl')
            expect(TrainingsWorkersCtrl).toBeDefined()
        }))
    })
})