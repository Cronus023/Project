'use strict';
describe('myApp.trainings.visit module', function() {
    beforeEach(module('myApp.trainings.visit'))
    describe('trainings.visit controller', function(){
        it('should ....', inject(function($controller) {
            //spec body
            const LoginCtrl = $controller('VisitTrainingsCtrl')
            expect(LoginCtrl).toBeDefined()
        }))
    })
})