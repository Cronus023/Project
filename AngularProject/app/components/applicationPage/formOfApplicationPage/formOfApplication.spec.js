'use strict';

describe('myApp.application.form module', function() {
    beforeEach(module('myApp.application.form'))
    describe('application.form controller', function(){
        it('should ....', inject(function($controller) {
            //spec body
            const FormApplicationCtrl = $controller('FormApplicationCtrl')
            expect(FormApplicationCtrl).toBeDefined()
        }))
    })
})