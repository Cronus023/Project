'use strict';
describe('myApp.application.reviewers.curriculum module', function() {
    beforeEach(module('myApp.application.reviewers.curriculum'))
    describe('application.reviewers.curriculum controller', function(){
        it('should ....', inject(function($controller) {
            //spec body
            const LoginCtrl = $controller('CurriculumReviewersApplicationCtrl')
            expect(LoginCtrl).toBeDefined()
        }))
    })
})