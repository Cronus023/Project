'use strict';
describe('myApp.application.finalDecision module', function() {
    beforeEach(module('myApp.application.finalDecision'))
    describe('application.finalDecision controller', function(){
        it('should ....', inject(function($controller) {
            //spec body
            const LoginCtrl = $controller('FinalDecisionApplicationCtrl')
            expect(LoginCtrl).toBeDefined()
        }))
    })
})